node_modules/
dist/
.env
.env.local
.env.production
.env.development
*.log
.expo/
generated/
# GQ-13 generation eval harness run outputs (local, machine-specific)
eval-runs/

# GQ-09 migration rollback snapshots (contain exercise data)
muscle-taxonomy-rollback-*.json

.DS_Store

# Claude Code private worktrees — entire nested checkouts (564MB as of 2026-09-17).
# Untracked but unignored means one `git add -A` commits three copies of this repo.
.claude/worktrees/
